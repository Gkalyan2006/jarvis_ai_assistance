# Minimal + Optional Requirements

This branch includes two requirement sets so you can do a quick, lightweight install for local desktop Jarvis, and later add heavy/optional packages only if you need them.

requirements-minimal.txt: minimal packages required to run the desktop GUI with VOSK wake-word and pyttsx3 TTS.
requirements-optional.txt: heavier ML/optional packages (faster-whisper, scipy) kept separate.
